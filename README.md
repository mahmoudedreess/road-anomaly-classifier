# road-anomaly-classifier
build a robust, reproducible pipeline to collect real road hazard imagery from the web and train a baseline HOG + SVM classifier to distinguish between potholes and road debris. This classifier will serve as the performance benchmark against which deep learning models in later milestones are evaluated.
